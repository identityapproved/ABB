TZ=UTC
# Optional: default ProtonVPN config filename present under /opt/openvpn-configs
# OPENVPN_CONFIG=nl-01.ovpn
# Optional: extra flags to pass to openvpn (space-delimited string)
# OPENVPN_EXTRA_ARGS=--pull-filter ignore "block-outside-dns"
