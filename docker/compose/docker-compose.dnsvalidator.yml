version: "3.8"

services:
  dnsvalidator:
    build:
      context: ../images/dnsvalidator
    container_name: dnsvalidator
    network_mode: "container:vpn-gateway"
    volumes:
      - ${DNSVALIDATOR_OUTPUT:-/opt/abb-docker/data/dnsvalidator}:/dnsvalidator/output
    working_dir: /dnsvalidator/output
    tty: true
